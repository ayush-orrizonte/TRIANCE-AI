<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Poppins" !important;
        background-color: #f3f4f6;
        color: #333;
        margin: 0;
        padding: 0;
      }

      .email-container {
        max-width: 600px;
        margin: 20px auto;
        background-color: #ffffff;
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }

      .header {
        background-color: #4c1d95;
        color: white;
        text-align: center;
        padding: 20px;
      }

      .header img {
        max-height: 50px;
      }

      .content {
        padding: 20px;
      }

      .content h1 {
        font-size: 1.5em;
        color: #4c1d95;
      }

      .otp-box {
        background-color: #e0e7ff;
        color: #3730a3;
        padding: 10px 20px;
        border-radius: 5px;
        font-weight: bold;
        text-align: center;
        margin: 20px 0;
        font-size: 1.2em;
        letter-spacing: 2px;
      }

      .plain-link {
        margin-top: 15px !important;
        font-size: 0.9em;
        color: #000 !important;
        word-break: break-all;
      }

      .plain-link a {
        color: blue !important;
        margin-top: 15px !important;
      }

      .footer {
        text-align: center;
        font-size: 0.9em;
        color: #000;
        padding: 10px 20px;
        background-color: #888;
      }
    </style>
  </head>

  <body>
    <div class="email-container">
      <div class="header">
        <img
          src="https://res.cloudinary.com/djoz0tmyl/image/upload/v1736183021/guru-mitra-logo_1_2_ddmrgs.png"
          alt="Logo"
        />
      </div>
      <div class="content">
        <h1>Dear <%= name %>,</h1>
        <p>Your One-Time Password (OTP) for verification is:</p>

        <div class="otp-box"><%= otp %></div>

        <p>This OTP is valid for the next 10 minutes. Do not share it with anyone.</p>

        <% if (locals.link) { %>
        <p class="plain-link">
          Need help? Visit: <br />
          <a
            href="<%= link %>"
            style="color: blue; margin-top: 15px; display: inline-block"
            ><%= link %></a
          >
        </p>
        <% } %>
      </div>

      <% if (locals.footer) { %>
      <div class="footer">
        <p><%= footer %></p>
      </div>
      <% } %>
    </div>
  </body>
</html>
 @Amulya templelate for otp , check kr lena