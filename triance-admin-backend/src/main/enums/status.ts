export enum MenuStatus {
  INACTIVE = 0,
  ACTIVE = 1,
  DELETED = 2,
}

export enum AdminStatus {
  ACTIVE = 1,
  INACTIVE = 2,
  BLOCKED = 3,
}

export enum UserStatus {
  ACTIVE = 1,
  INACTIVE = 2,
  BLOCKED = 3,
}

export enum Levels {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum RoleStatus {
  INACTIVE = 0,
  ACTIVE = 1,
  ARCHIVED = 2,
}